services:
  - type: web
    name: ecommerce-flask
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ecommerce_flask_db_user
          property: connectionString


