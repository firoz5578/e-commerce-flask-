name: Python CI

on:
  push:
    branches: [ firoz ]
  pull_request:
    branches: [ firoz ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Flask app (just a demo)
      run: |
        echo "🎉 Flask app setup done"

